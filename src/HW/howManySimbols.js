/* eslint-disable no-trailing-spaces */
// Вывести в консоль суммарное количество симоволов в двух строках
export function howManySimbols(str1, str2) {
  const result = str1.length + str2.length;
  console.log(result);
  return result;
}
